apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phoenix-todo.fullname" . }}-test-connection"
  labels:
    {{- include "phoenix-todo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phoenix-todo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
